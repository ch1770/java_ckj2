package day02;

import java.util.Scanner;

public class ex12_swithbreak {
    public static void main(String[] args) {
	     Scanner sc=new Scanner(System.in);
	     System.out.println("정수입력:");
	
	//입력받은 정수를 5로 나눈 나머지
	//2보다 작으면 "*"출력
	//그렇지 않으면  "**" 출력
	//-switch문을 사용하여 작성하시오
	//*switch 문안에서 break를 쓰지 않으면  
	//저건에 맞는 case 영역뿐만 아니라  이후의 코드도 실행된다 
	     switch (num%5) {
	     case 0:
	    	 System.out.println(h);r value:
	        	 break;
	         
	         de
	     }
	sc.close();
   }
}
